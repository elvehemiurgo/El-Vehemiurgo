{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git -C ../El-Vehemiurgo pull --ff-only 2>&1 | tail -1; echo \"VEHEMIURGIA @ $(git -C ../El-Vehemiurgo log -1 --format='%h %s' 2>/dev/null)\"; echo \"último notebook: $(ls ../El-Vehemiurgo/notebook/ 2>/dev/null | grep -v README | tail -1)\""
          }
        ]
      }
    ]
  }
}
